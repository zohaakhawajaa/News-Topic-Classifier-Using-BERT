# News-Topic-Classifier-Using-BERT
Fine-tuned a BERT-based transformer (bert-base-uncased) to classify news headlines into topic categories using the AG News dataset. Implemented tokenization and preprocessing, evaluated performance with accuracy and F1-score, and deployed the model using Streamlit/Gradio for real-time interaction.
